# UI/UX Skill 包升级日志

## v2.1.0 (2026-01-27) - 核心架构升级

### ✨ 新增功能
- **新增 Context 全局状态** - core/context.ts，提供用户、菜单、权限等全局状态管理
- **新增工具函数层**
  - utils/request.ts - HTTP请求封装，支持认证、错误处理、多种响应类型
  - utils/env.ts - 环境判断 (prod/gray/pre/daily/local)
  - utils/storage.ts - localStorage封装，支持token存取
  - utils/channel.ts - 跨Tab通信 (BroadcastChannel API)
- **新增常量配置层**
  - constants/index.ts - 权限码映射、应用类型、默认图片等
  - constants/menu.ts - 默认菜单结构配置
  - constants/spin.tsx - 统一加载图标配置
- **新增业务组件**
  - RouteWrapper.tsx - 路由包装器，支持Suspense懒加载
  - AuthWrapper.tsx - 权限包装器，统一处理按钮和功能的权限控制
  - WorkSpaceSelect.tsx - 工作空间选择器，支持切换、申请、创建功能
- **新增核心类型定义** - types/common.ts，包含 User, Menu, WorkSpaceItem, AuthCode 等类型

### 📦 功能增强
- **Layout 组件升级**
  - 支持路由权限验证 (enableAuth 配置)
  - 支持 useOutlet 配置，灵活选择是否使用 Outlet
  - 完善背景图引用方式
  - 添加 ManualLayout 组件，支持手动模式选择
  - 集成 Context 状态

### 📁 新增文件结构
```
skills/ui-ux/
├── core/                # 核心架构
│   └── context.ts              # 全局Context状态管理
├── constants/           # 常量配置
│   ├── index.ts                # 权限码、应用类型等
│   ├── menu.ts                 # 默认菜单配置
│   └── spin.tsx                # Spin加载图标
├── utils/               # 工具函数
│   ├── request.ts              # HTTP请求封装
│   ├── env.ts                  # 环境判断工具
│   ├── storage.ts              # 本地存储工具
│   └── channel.ts              # 跨Tab通信工具
├── components/          # 新增业务组件
│   ├── RouteWrapper.tsx        # 路由包装器
│   ├── AuthWrapper.tsx         # 权限包装器
│   └── WorkSpaceSelect.tsx     # 工作空间选择器
└── types/               # 新增类型定义
    └── common.ts               # 核心类型定义
```

### 🎯 解决的问题
- 缺少核心架构层导致无法完全复制项目
- 布局组件无法与 Context 集成
- 缺少请求封装和工具函数
- 缺少权限管理组件
- 缺少工作空间切换功能

---

## v2.0.0 (2026-01-27) - 重大升级

### ✨ 新增功能
- **新增 Nav 组件** - 侧边导航组件，支持菜单分组和激活状态高亮
- **新增 Header 组件** - 头部导航组件，支持用户信息和下拉菜单
- **新增 Layout 组件** - 布局容器组件，提供 CommonLayout 和 SimpleLayout
- **新增全局样式** - styles/global.css，包含导航激活状态、滚动条美化等
- **新增图片资源** - assets/ 目录，包含 logo、背景图、banner 图等
- **新增类型定义** - types/assets.d.ts，提供图片资源类型声明
- **扩展 Banner 类型** - 支持 11 种 Banner 类型（新增 endpoint、plugins、observe、dataset、dimension、tasks）

### 🐛 问题修复
- 修复 Banner 组件图片引用路径问题
- 修复 Nav 激活状态样式缺失问题
- 修复图标显示问题（通过全局样式和资源文件）

### 📝 文档更新
- 更新 SKILL.md，添加新组件使用说明
- 更新 README.md，完善使用指南
- 添加 package.json，规范化包结构

### 📁 文件结构
```
skills/ui-ux/
├── assets/              # 图片资源
│   ├── logo.png                # Logo图标
│   ├── menuBg.png              # 侧边菜单背景图
│   ├── headerBg.png            # 头部背景图
│   ├── user.png                # 默认用户头像
│   └── banner/                 # Banner横幅图片
│       ├── application.png
│       ├── mcp.png
│       ├── moxingguangchang.png
│       ├── pingceshujuji.png
│       ├── zaixiantuili.png
│       ├── chajianguangchang.png
│       ├── jieruguancedian.png
│       └── helper.png
├── components/          # 自定义组件
│   ├── BaseTable.tsx           # 统一表格组件
│   ├── Tag.tsx                 # 自定义标签组件
│   ├── SliderInput.tsx         # 滑块输入组件
│   ├── spin.tsx                # 加载动画配置
│   ├── Banner.tsx              # 页面横幅组件
│   ├── BannerConstants.ts      # 横幅配置
│   ├── Nav.tsx                 # 侧边导航组件
│   ├── Header.tsx              # 头部导航组件
│   └── Layout.tsx              # 布局组件
├── hooks/               # 自定义 Hooks
│   └── useTitle.ts             # 页面标题管理
├── styles/              # 样式文件
│   └── global.css              # 全局样式（包含Nav激活状态等）
├── types/               # 类型定义
│   └── assets.d.ts             # 图片资源类型声明
├── example/             # 使用示例
│   └── ExamplePage.tsx         # 完整页面示例
├── SKILL.md            # Skill 主文档
├── README.md           # 组件库说明文档
└── package.json        # 包配置文件
```

### 🚀 使用方式
1. **复制全部文件** - 将整个 `skills/ui-ux/` 目录复制到项目中
2. **引入全局样式** - 在入口文件中引入 `styles/global.css`
3. **安装依赖** - `pnpm add antd @iconify/react react-router`
4. **使用组件** - 按照文档示例使用各组件

### 🎯 解决的问题
- 图片资源缺失导致的页面样式问题
- 导航激活状态样式不生效问题
- 图标显示异常问题
- 布局结构不完整问题
- 样式文件缺失问题

### 📊 技术栈
- UI框架: Ant Design 6.1.1
- 样式方案: TailwindCSS 4.1.18 + 自定义主题 + 全局样式
- 图标库: @iconify/react 6.0.2
- 字体: 系统字体 + 钉钉进步体
- 图片资源: 内置Logo、背景图、Banner图等